Always read the global `AGENTS.md` (synced to `~/.claude/CLAUDE.md` and `~/.codex/AGENTS.md`) before any repo-specific instructions.

For implementation work, read `docs/VERIFY.md` when present and use or update `scripts/self-test.sh` as the repo-local proof entrypoint.
